# AICoding 架构设计 · 系统设计

> 本文档为《AICoding 架构设计》核心产物之一，对应**系统架构设计模版**。
> 上游输入：《高层架构设计》产出的业务架构、系统定位、能力盘点；
> 下游输出：用于驱动《部署设计》《安全设计》《UserStory》的实施。

> **模版使用约定（按需裁剪）**：
> - 若实际系统不涉及某章节内容（例如：无缓存则跳过 §4.4 缓存与 Key 规范、无存量数据迁移则跳过 §4.5 数据迁移与兼容性、无横切公共能力则跳过 §3.4 跨模块公共能力），可**整节略过**，并在 §0 修订记录中注明"未启用：§x.x（理由：...）"；
> - 略过章节后，**后续章节序号顺延**（如 §4.4 略过则原 §4.5 顺延为 §4.4），保证最终文档编号连续；
> - 跨章节引用必须同步更新（如 §5 / §6 中对 §3.5.4 的引用），禁止悬空引用；
> - **不可裁剪的章节**：§1 引言、§2 业务架构、§3.1 应用架构概览、§3.2 模块详细设计、§4.1~§4.3 数据库设计核心三节、§5 部署架构（除非系统不部署）、§7 安全设计（合规底线）。

---

## 0. 元信息：修订记录

> 记录文档版本、变更内容、修订人、修订时间，确保设计文档的可追溯。

| 文档版本 | 发布日期 | 修订人 | 修订说明 |
| --- | --- | --- | --- |
| v1.0 | YYYY-MM-DD | `<姓名>` | 文档初始版本 |

**填写要求**：每次评审通过新增一行；修订说明必须能定位到具体章节，禁止"细节优化 / 调整内容"等不可验证表述。

---

## 1. 引言

### 1.1 文档目标

明确本文档的设计范围、读者群体与设计目标。

- **一句话定位**：本文档定义 `<系统名>` 在交付物中的所有架构设计相关产物。
- **读者对象**：架构师 / 开发 / 运维 / 安全 / 评审委员会。
- **设计范围**：业务架构 + 应用架构 + 数据库设计 + 部署架构 + 网络架构 + 安全设计 + 可观测设计。

### 1.2 关联文档清单

列出与本设计配套的所有上下游文档（高层架构、UserStory、部署设计、安全设计等）以及本文档撰写所**依赖的输入信息**。

| 输入类型 | 内容要点 | 在本文档中的用途 | 形式（外部文档 / 本文档自带） |
| --- | --- | --- | --- |
| 业务需求 | 业务定位、角色、场景、关键流程 | 第 2 章业务架构 | `<...>` |
| 非功能性需求 | QPS、并发、数据量、可用性、RTO/RPO、合规等级 | 第 3.5 / 5 / 7 章 | `<...>` |
| 系统定位与边界 | 本系统在企业整体架构中的位置、与友邻系统的关系 | 第 3.1 集成架构 | `<...>` |
| 外部系统 API | 第三方平台 / 友邻业务系统的协议、版本、配额 | 第 3.1.3 / 3.2 模块 | `<...>` |
| 云组件 / 中间件清单 | 团队可用 / 可申请的中间件清单与规格基线 | 第 3.1.3 / 4.3 / 5 / 6 章 | `<...>` |
| 团队 / 行业规范 | 命名、数据库、安全、部署规范 | 第 4.1 / 5 / 7 章 | `<...>` |

**填写要求**：
- 每条输入要写明"具体内容要点"，禁止只写文档名而不说提供了什么；
- 若该输入暂无外部文档承载，须在"形式"列写明 `本文档自带`，并在对应章节直接补全内容；
- 后续章节如需追溯某项数据来源，应能在本表查到对应输入。

### 1.3 名词与术语表

统一系统中专有名词、缩写的中英文对照与含义。本表是 **业务名 ↔ 代码对象 ↔ 数据表** 三层映射的唯一索引。

| 业务术语 | 业务定义（≤ 30 字） | 应用层核心对象（§3.3 O-xx） | 数据库表名（§4.2） |
| --- | --- | --- | --- |
| `<业务术语1>` | `<业务上的明确定义>` | `<O-xx 对象名，如 Customer>` | `<t_customer>` |
| `<业务术语2>` | `<...>` | `<...>` | `<...>` |

**填写要求**：
- §2 业务架构中出现的所有名词、§3 模块名与核心业务对象名、§4 数据表所代表的核心实体，全部能在术语表中查到且只有一个标准名；
- 任意一行的"业务术语 → 核心对象 → 表名"映射可双向追溯。

---

## 2. 业务架构

> **本章回答**：系统服务什么业务、业务由哪些场景构成、业务的关键流程长什么样。
> **本章不回答**：用什么技术、怎么部署、表怎么建。

### 2.1 业务架构概览

#### 2.1.1 业务全景图

> 系统级业务全景图。以**业务域分块**展示系统的业务结构，块与块之间用箭头标注业务上下游、依赖、衍生关系。

**业务域清单**：

| 业务域 | 核心场景 | 涉及角色 | 与其他业务域的关系 |
| --- | --- | --- | --- |
| `<业务域1>` | `<场景1>` / `<场景2>` | `<角色A>` / `<角色B>` | 上游 `<域X>` / 下游 `<域Y>` |
| `<业务域2>` | `<...>` | `<...>` | `<...>` |

**填写要求**：业务域 3~7 个为宜；业务域名称必须与第 3 章模块、第 4 章数据库分组保持一致。

### 2.2 领域关系映射

#### 2.2.1 限界上下文清单

每个业务域对应一个限界上下文（Bounded Context），明确该域内"业务实体的标准定义"：

| 限界上下文 | 对应业务域 | 域类型 | 覆盖的核心业务实体 | 上下文边界（属于本域 vs 不属于） |
| --- | --- | --- | --- | --- |
| `<域1-BC>` | `<业务域1>` | 核心域 | `<实体1>` / `<实体2>` | 属于：`<...>`；不属于：`<...>` |
| `<域2-BC>` | `<业务域2>` | 支撑域 | `<实体3>` | `<...>` |
| `<域3-BC>` | `<业务域3>` | 通用域 | `<实体4>` | `<...>` |

**域类型说明**：

| 域类型 | 含义 | 投入策略 |
| --- | --- | --- |
| 核心域（Core） | 业务护城河，差异化竞争力所在 | 投入最多资源自研 |
| 支撑域（Supporting） | 必要但非差异化 | 可自研可外采 |
| 通用域（Generic） | 通用能力（如用户中心、消息中心） | 优先复用 / 外采 |

#### 2.2.2 上下文映射（Context Mapping）

| 关系编号 | 上游上下文 | 下游上下文 | 关系模式 | 同步方式 | 选择理由 |
| --- | --- | --- | --- | --- | --- |
| C-01 | `<域1-BC>` | `<域2-BC>` | Customer/Supplier | API 同步 / 消息异步 | 上游配合下游需求节奏 |
| C-02 | `<外部系统>` | `<域1-BC>` | ACL 防腐层 | API + 适配器 | 外部模型不污染本域 |
| C-03 | `<域1-BC>` | `<域3-BC>` | Shared Kernel | 共享 DTO / SDK | 公共概念稳定且必须一致 |
| C-04 | `<上游平台>` | `<本系统>` | Conformist | 直接遵循对方模型 | 无议价能力的强势上游 |
| C-05 | `<域2-BC>` | `<域3-BC>` | Published Language | 消息事件 / 标准协议 | 多个下游共同消费 |

**关系模式说明**：

| 关系模式 | 适用场景 |
| --- | --- |
| Customer/Supplier | 上下游强协作，下游能影响上游迭代节奏（如：订单 → 支付） |
| Conformist | 顺从者，下游完全遵循上游模型（如：对接微信开放平台） |
| ACL（Anticorruption Layer） | 在本域和外部域之间建一层适配，外部模型变化不影响本域（对接外部强势平台时必用） |
| Shared Kernel | 共享内核，多个上下文共享一小块代码 / 模型；维护成本高，慎用 |
| Published Language | 上游以标准协议（事件 / Schema）对外暴露，多下游消费 |
| Open Host Service | 上游提供标准 API 接口，类似 Published Language 但通过 RPC |

#### 2.2.3 跨域协作原则

| 原则编号 | 原则 |
| --- | --- |
| P-01 | 核心域 → 支撑域 / 通用域：禁止反向依赖（核心域不依赖支撑域的内部模型） |
| P-02 | 核心域之间：禁止直接耦合，必须通过事件 / API 解耦 |
| P-03 | 对接外部不可控系统：必须建 ACL 防腐层，禁止把外部模型贯穿到本域 |

### 2.3 详细业务架构

#### 2.3.1 用例图

> 体现"角色 — 用例 — 系统边界"。每个一级业务场景一张用例图。建议使用 PlantUML / Mermaid 文本格式存仓库。

**用例图清单**：

| 编号 | 用例图标题 | 涉及业务域 | 源文件位置 |
| --- | --- | --- | --- |
| UC-01 | `<用例图1>` | `<业务域1>` | `<docs/uml/uc-01.puml>` |
| UC-02 | `<...>` | `<...>` | `<...>` |

#### 2.3.2 业务流程图

> 关键业务以**角色泳道图**描述核心流程。横向是流程节点，纵向是角色（含外部系统）。判定节点（菱形）必须有明确的分支条件文字标注，禁止"是 / 否"无语义标签。

**关键流程清单**（核心业务覆盖度 ≥ 80%）：

| 流程编号 | 流程名 | 起点 | 终点 | 涉及业务域 | 源文件位置 |
| --- | --- | --- | --- | --- | --- |
| BF-01 | `<...>` | `<...>` | `<...>` | `<...>` | `<...>` |
| BF-02 | `<...>` | `<...>` | `<...>` | `<...>` | `<...>` |

### 2.4 业务范围与边界

> **本节回答**：本期到底要做哪些功能、不做哪些功能，避免"边界蔓延"。

#### 2.4.1 功能模块清单

按"功能编号 → 子功能编号"两层组织，与 §2.1 业务域对应。

| 编号 | 一级功能名 | 子功能描述 | 对应业务域 | 实现状态 |
| --- | --- | --- | --- | --- |
| F1 | `<一级功能名>` | — | `<业务域1>` | — |
| F1.1 | — | `<子功能描述>` | `<业务域1>` | 完整实现 |
| F1.2 | — | `<子功能描述>` | `<业务域1>` | `[MOCK]` `<降级理由 / 完整版替换路径>` |
| F2 | `<一级功能名>` | — | `<业务域2>` | — |
| F2.1 | — | `<子功能描述>` | `<业务域2>` | 完整实现 |
| F3 | `<公共能力>` | — | 跨域 | — |

**编号规则**：

| 规则项 | 取值 |
| --- | --- |
| 一级编号 | `F1 / F2 / ...`，与一级业务域一一对应 |
| 子功能编号 | `F1.1 / F1.2 / ...`，互不重叠 |
| Mock / 简化标注 | 必须显式标注 `[MOCK]` 或 `[简化]`，并附"完整版替换路径" |

**In-Scope（本期范围内）**：

| 编号 | 本期必做的事项 |
| --- | --- |
| F1 | `<本期必做的核心功能 1>` |
| F2 | `<...>` |
| N1 | `<本期必达的非功能性指标，如：单租户支撑 5 万用户>` |

**Out-of-Scope（本期不做）**：

| 编号 | 不做的事项 | 不做的原因 | 未来归属 |
| --- | --- | --- | --- |
| O1 | `<...>` | `<本期范围 / 待业务确认 / 技术依赖未就绪>` | `<二期 / 完整版 / 永不做>` |
| O2 | `<...>` | `<...>` | `<...>` |

**填写要求**：In-Scope ≤ 15 条；Out-of-Scope ≥ 3 条（少于 3 条说明边界没想清楚）。

### 2.5 质量与柔性可用

#### 2.5.1 服务质量目标

> 按 ISO 25010 选出本系统**最重要的 3-5 个**质量目标（必须排序），并落到可验证场景。

**质量目标 Top 3-5**：

| 优先级 | 质量目标 | 业务驱动 | 受影响的关键章节 |
| --- | --- | --- | --- |
| Q1 | `<例：高可用 99.95%>` | `<客户合同 SLA>` | §5.2 / §5.3 / §4.3 |
| Q2 | `<例：低延迟 P99<200ms>` | `<用户体验阈值>` | §3.1 / §4.4 / §5.5 |
| Q3 | `<例：可扩展性，1 年内支持 10x 数据量>` | `<业务增长预测>` | §4.2 / §5.5 |
| Q4 | `<例：可维护性，新人 1 周上手>` | `<团队规模>` | §3.1 / §3.2 |
| Q5 | `<例：合规性（GDPR / 等保 2.0）>` | `<法规要求>` | §7 |

**可验证质量场景**（参考 SEI ATAM 方法）：

| 场景编号 | 关联目标 | 触发源 | 触发条件 | 期望响应 | 度量指标 |
| --- | --- | --- | --- | --- | --- |
| QS-01 | Q1 | 单可用区故障 | 任一 AZ 节点全部宕机 | 自动切换至备用 AZ | RTO ≤ 5min；错误率峰值 ≤ 1%；持续 ≤ 60s |
| QS-02 | Q2 | 用户访问 | 列表接口 1000 QPS 并发 | 正常返回 | P95 ≤ 150ms；P99 ≤ 200ms |
| QS-03 | Q3 | 业务增长 | 数据量 1000 万 → 1 亿条 | 无需重构核心表 | 改造工期 ≤ 5 人天 |


#### 2.5.2 业务柔性可用策略

> 故障态下"保住什么、放弃什么"的取舍清单。核心业务永不降级；外围功能优先牺牲；用户感知必须显式可控。

**业务功能优先级分层**（§2.4.1 中每个一级功能 F-x 必须显式标注一个层级）：

| 层级 | 含义 | 故障态下的处置方向 | 用户感知 |
| --- | --- | --- | --- |
| L0 核心业务 | 系统的生命线，挂掉等于业务停摆 | 不降级；优先消耗所有资源保障 | 无 / 极轻微 |
| L1 重要业务 | 不影响主链路但用户高频使用 | 限流 + 排队 + 异步化 | 变慢 / 排队提示 |
| L2 辅助业务 | 提升体验但非必要 | 关闭 / 返回兜底数据 | 部分功能不可用 + 友好提示 |
| L3 附加业务 | 长尾 / 数据类 / 统计类 | 直接关闭 + 异步补偿 | 通常无感 |

**业务降级清单**：

| 编号 | 关联功能（F-xx） | 触发条件 | 降级动作 | 用户感知 | 决策类型 |
| --- | --- | --- | --- | --- | --- |
| BD-01 | `<F1.2>` | `<可检测的指标 / 错误率 / 水位线 / 依赖状态>` | `<切换通道 / 返回兜底数据 / 异步补偿>` | `<一句话用户视角描述>` | 自动 / 手动 |
| BD-02 | `<...>` | `<...>` | `<...>` | `<...>` | 自动 / 手动 |

**填写要求**：
- §2.4.1 全部一级功能均已分层（L0~L3）；L0 数量 ≤ 总功能数的 30%；
- 用户感知列禁止出现"走兜底逻辑 / 熔断"等技术词。

---

## 3. 应用架构

> **本章回答**：系统由哪些模块组成、模块与外部系统怎么集成、每个模块的内部交互链路是什么。
> **本章是系统设计的核心**，章节下应当占整个文档篇幅的 40%~50%。

### 3.1 应用架构概览

#### 3.1.1 系统上下文

本系统作为**中心节点**，周围辐射出"用户角色 + 外部业务系统 + 上游 / 下游平台"关系。

**系统上下文要素清单**：

| 节点类型 | 节点名 | 与本系统的关系 | 关系语义 |
| --- | --- | --- | --- |
| 角色 | `<管理员>` | 使用 | 管理端操作 |
| 角色 | `<业务人员>` | 使用 | 工作台操作 |
| 上游平台 | `<E-04>` | 推送 → 本系统 | `<...>` |
| 外部业务系统 | `<E-01 身份认证>` | 双向调用 | `<...>` |
| 第三方平台 | `<E-03 支付平台>` | 本系统 → 调用 | `<...>` |
| 下游平台 | `<E-06>` | 本系统 → 推送 | `<...>` |

**填写要求**：
- 本系统画成单一节点，**不展开内部组件**（内部组件归 §3.1.2）；
- 周围节点取自 §3.1.3 外部依赖清单（E-xx）与 §2.3 用例图角色；
- 每条边必须标注关系语义（使用 / 双向调用 / 数据推送 / 订阅 等）。

#### 3.1.2 系统模块图

- 分层架构图，每一层标出关键组件与协议方向
- **接入层**：管理端 / 工作台 / 移动端 / 开放 API / Webhook 接收端
- **应用层**：覆盖业务模块全景
- **中间件层**：覆盖基础设施依赖清单
- **集成对接点**：覆盖外部依赖清单集成关系

**分层组件清单**：

| 层次 | 内容 | 数据来源 |
| --- | --- | --- |
| 接入层 | 管理端 / 工作台 / 移动端 / 开放 API / Webhook 接收端 | §2.3 用例图角色 |
| 应用层 | 按 §2.1 业务域划分的后端服务模块（与 §3.2 一一对应） | §2.1 / §3.2 |
| 中间件层 | 数据库 / 缓存 / 消息队列 / 任务调度 / 搜索引擎（C-xx） | §3.1.3 云组件清单 |
| 集成对接层 | 与外部系统的对接桩 / SDK 封装入口（E-xx） | §3.1.3 外部依赖清单 |

**填写要求**：层次自上而下展示；每条线必须标注协议（HTTP / gRPC / MQ / SDK / Webhook 等）；中间件 / 外部系统组件块上标注其编号（C-xx / E-xx）。

#### 3.1.3 集成架构

> **核心区分**：本节区分两类"对外依赖"——
> - **外部依赖（E-xx）**：跟别人协作，本系统通过 API / SDK / 消息与之集成的业务系统、第三方平台、内部友邻系统；
> - **云组件（C-xx）**：自己用的资源，本系统运行所需的中间件与云资源（数据库、缓存、对象存储、容器编排等）。

##### 外部依赖清单

| ID | 外部系统 | 归属团队 / 供应商 | 类别 | 使用方式 | 集成方式 | 协议 / 版本 | 功能定位（≤ 30 字） | 联系人 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E-01 | `<身份认证平台>` | `<团队/厂商>` | 身份认证 | 消费 | REST API | OAuth 2.0 | 登录态校验 / 用户体系 | `@<姓名>` |
| E-02 | `<内部 CRM>` | `<业务部门>` | 业务协作 | 双向 | REST API + Webhook | v2.3 | 客户数据同步 | `@<姓名>` |
| E-03 | `<支付平台>` | `<外部厂商>` | 支付 | 消费 | SDK | v3.1 | 在线支付 / 退款 | `@<姓名>` |
| E-04 | `<埋点平台>` | `<...>` | 埋点 | 生产 | SDK | `<...>` | 用户行为采集 | `@<姓名>` |

**填写要求**：
- 类别取值（封闭枚举）：身份认证 / 业务协作 / 支付 / 通知推送 / 埋点 / 第三方业务平台 / 监控审计 / 文件导出；
- 使用方式取值：消费（本系统调用对方）/ 生产（对方调用 / 订阅本系统）/ 双向。

##### 云组件 / 基础设施清单

| ID | 组件类别 | 选型 | 部署形态 | 规格量级 | 容量预估 | 提供方 / SLA | 用途 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C-01 | 关系型数据库 | `<MySQL 8.0>` | 主从 / 集群 | `<2C8G × 3>` | `<数据量 / QPS>` | `<云厂商；SLA 99.95%>` | 业务主数据 |
| C-02 | 缓存 | `<Redis 7.0>` | 主从 / 哨兵 | `<2C4G>` | `<内存量 / QPS>` | `<云厂商>` | 会话 / 去重 / 分布式锁 |
| C-03 | 消息队列 | `<Kafka>` | 多副本集群 | `<分区数>` | `<TPS / 堆积量>` | `<云厂商>` | 异步解耦 |
| C-04 | 搜索引擎 | `<Elasticsearch 8.x>` | 多节点集群 | `<...>` | `<...>` | `<...>` | 全文检索 |
| C-05 | 对象存储 | `<S3 / COS / OSS>` | 多 AZ | — | `<容量增长>` | `<云厂商>` | 文件 / 备份 |
| C-06 | 容器编排 | `<K8s>` | 多 AZ | `<Node 数 × 规格>` | `<Pod 数>` | `<云厂商 / 自建>` | 服务部署 |
| C-07 | API 网关 | `<APISIX / 云 API 网关>` | `<...>` | `<...>` | `<QPS>` | `<...>` | 入口路由 / 鉴权 / 限流 |
| C-08 | 任务调度 | `<XXL-Job>` | `<...>` | `<...>` | `<Job 数>` | `<...>` | 定时任务 |
| C-09 | 日志服务 | `<ELK>` | `<...>` | `<...>` | `<日志量 / 天>` | `<...>` | 应用日志 / 审计日志 |
| C-10 | 监控告警 | `<Prometheus + Grafana>` | `<...>` | — | — | `<...>` | 指标采集 / 告警 |
| C-11 | 链路追踪 | `<SkyWalking>` | `<...>` | — | `<采样率>` | `<...>` | 分布式调用追踪 |
| C-12 | 密钥管理 | `<KMS>` | `<...>` | — | — | `<云厂商>` | 密钥托管 |

**填写要求**：选型必须含**具体产品 + 版本号**；规格量级 / 容量预估必须有数字；不使用的组件**整行删除**，禁止填"不适用"。

##### 组件依赖关系

以**矩阵形式**呈现"哪个业务模块用了哪些外部依赖"，分析依赖关系及故障传导风险：

| 业务模块（§3.2） | 依赖的外部系统（E-xx） | 依赖的云组件（C-xx） | 故障传导风险 |
| --- | --- | --- | --- |
| `<模块1>` | E-01, E-02 | C-01, C-02, C-03 | E-01 不可用 → 全站不可登录 |
| `<模块2>` | E-03 | C-01, C-05 | E-03 超时 → 支付链路降级 |
| `<模块3>` | — | C-01, C-04 | C-04 不可用 → 检索降级到 DB |

**填写要求**：编号必须与外部依赖清单 / 云组件清单完全对应；§3.2 模块详细设计的"涉及的内外部系统"必须能从本矩阵中查到。

#### 3.1.4 工程结构

- 明确项目的物理组织方式
- 公共模块（`common`）与业务服务的依赖方向必须清晰：**业务服务依赖 common，禁止反向**

```
<项目名>/
├── backend/                 # 后端工程根目录
│   ├── common/              # 公共模块：DTO / VO / Enum / Exception / Constants
│   ├── <service-1>/         # 业务服务 1
│   ├── <service-2>/         # 业务服务 2
│   └── <service-N>/         # 业务服务 N
└── frontend/                # 前端工程根目录
    ├── <端1>/               # 例如：管理端 / 工作台
    │   ├── src/
    │   │   ├── api/         # 接口封装（与后端 §3.2 接口清单对齐）
    │   │   ├── stores/      # 全局状态
    │   │   ├── pages/       # 页面（按路由分目录）
    │   │   ├── components/  # 跨页面通用组件
    │   │   ├── hooks/       # 自定义 Hooks
    │   │   ├── types/       # TS 类型定义（与后端 DTO/VO 对齐）
    │   │   ├── utils/       # 工具函数
    │   │   ├── sdk/         # 三方 SDK 封装（对应 §3.1.3 E-xx）
    │   │   └── routes.tsx   # 路由配置
    │   └── package.json
    ├── <端2>/               # 例如：移动端 / H5
    └── shared/              # 跨端共用组件 / 类型定义
```

#### 3.1.5 技术选型（版本约束）

| 技术分类 | 选型 | 版本 | 备注 / 选型理由 |
| --- | --- | --- | --- |
| 后端语言 | `<Java / Go / Python>` | `<...>` | 团队规范固定 |
| 后端框架 | `<Spring Boot / Gin>` | `<...>` | `<选型理由>` |
| 持久层（ORM） | `<MyBatis-Plus / GORM>` | `<...>` | `<...>` |
| 关系型存储 | `<MySQL / PostgreSQL>` | `<...>` | `<...>` |
| 缓存 | `<Redis>` | `<...>` | 用途：会话 / 去重 / 分布式锁 |
| 消息队列 | `<Kafka / RabbitMQ>` | `<...>` | 用途：异步解耦 / 事件驱动 |
| API 通信 | `<REST / gRPC>` | — | 协议选择理由 |
| 前端框架 | `<React / Vue>` + `<TypeScript>` | `<...>` | SPA / SSR |
| 前端状态 | `<Redux / Zustand / Pinia>` | `<...>` | 体积 / TS 支持 / 学习成本 |
| 前端构建 | `<Vite / Webpack>` | `<...>` | `<...>` |
| 认证方案 | `<JWT / OAuth2 / SSO>` | — | 见 §7.2 |
| 三方 SDK | `<SDK 名>` | `<...>` | 用途与替代方案 |

**填写要求**：必须有版本号；选型理由用一句话说明（**为什么选 A 不选 B**），避免"团队习惯"这种空话。

### 3.2 模块详细设计

> **核心方法论**：模块详细设计要画的是**业务逻辑在角色和系统之间流动的过程**，不是接口实现细节。
> **组织原则**：先在 §3.2.1 / §3.2.2 给出跨模块共享约束与模块清单；§3.2.3 给出"单模块设计模板（五段式）"；之后**每个模块在 §3.2.M{N} 下独立成节**，按模板展开。

#### 3.2.1 模块公共约束（Common）

**通信方式约束**：

| 约束项 | 取值 |
| --- | --- |
| 接口路径风格 | RESTful / RPC（二选一并锁定） |
| 协议 | HTTP / gRPC / MQ |
| 数据格式 | JSON / Protobuf |
| 字符编码 | UTF-8 |

**通用基础结构**：

| 基类 / 结构 | 用途 | 包路径 |
| --- | --- | --- |
| `BaseRequest` | 多态请求对象基类 | `com.xxx.common.dto` |
| `BaseResponse` | 多态响应对象基类 | `com.xxx.common.dto` |
| `PageRequest` | 分页请求 | `com.xxx.common.dto` |
| `PageResponse<T>` | 分页响应 | `com.xxx.common.dto` |
| `Result<T>` | 统一返回结构（含 code / msg / data / traceId） | `com.xxx.common.dto` |

#### 3.2.2 模块清单与编号规则

> 在展开每个模块的详细设计前，先在此登记本系统所有模块。每个模块**必须**按 §3.2.3 的"五段式"模板独立成节。

**模块清单**：

| 编号 | 模块名 | 业务域（§2.1） | 模块负责人 | 关联子领域 |
| --- | --- | --- | --- | --- |
| M1 | `<模块1>` | `<...>` | `@<姓名>` | `<...>` |
| M2 | `<模块2>` | `<...>` | `@<姓名>` | `<...>` |
| M3 | `<模块3>` | `<...>` | `@<姓名>` | `<...>` |

**编号规则**：
- 模块编号 `M{N}` 全局唯一，与 §2.1 业务域名一致；
- 每个模块在 §3.2 下独立成节，编号 `§3.2.M{N}`；
- 节内五段式编号 `§3.2.M{N}.1` ~ `§3.2.M{N}.5`，顺序与 §3.2.3 模板一致。

#### 3.2.3 单模块设计模板（五段式）

> ⚠️ **本节性质说明（写给使用本模版的人 / AI）**
>
> 本节定义"每个模块在 §3.2.M{N} 下五段各应该**产出什么内容**、**遵守什么格式**"。**本节本身不是任何模块的内容**——以下所有"硬性要求表 / 示意 DTO 代码 / 示意伪代码 / 示意表格行"都属于**规范说明与格式示意**，仅供理解之用。
>
> **§3.2.M{N} 下应当怎么填**：
> 1. **直接写该模块特有的业务内容**（真实接口、真实 DTO、真实时序图、真实流程）；
> 2. **不要把本节的"硬性要求表 / 示意代码 / 示意表格行"复制到任何 §3.2.M{N} 下**——本节列出的硬性要求是**所有模块共同遵守的项目级规范**，在各模块下**只体现、不重复声明**；
> 3. 五段顺序固定；简单 CRUD 模块可省略 `.5 关键流程逻辑`，但需在 `.5` 标题下显式注明"无复杂状态机/事务/跨多步异步，本段省略"，禁止留空标题。

##### §3.2.M{N}.1 模块概述

**该段产出物**：在 §3.2.M{N}.1 下输出本模块的"业务定位三要素"——位置 / 角色 / 内外部系统。

**硬性要求**（所有模块共同遵守，**不在 §3.2.M{N} 下重复声明这张表**）：

| 要素 | 内容 |
| --- | --- |
| 模块在业务流程中的位置 | 在哪个业务域的哪些场景里发挥作用 |
| 涉及的角色 | 业务角色（如管理员 / 业务人员 / 客户） |
| 涉及的内外部系统 | 列出本模块运行时会与之交互的所有内外部系统名（命名必须与 §3.1 一致） |

**示意措辞**（仅供书写参考，**不要照抄**）："`<模块名>` 模块覆盖 `<场景1>`、`<场景2>` 业务流程，业务逻辑在 `<角色A>`、`<角色B>` 之间流动，同时涉及 `<管理端>`、`<外部系统X>`、`<消息总线>` 等内外部系统的数据流动。"

##### §3.2.M{N}.2 接口清单

**该段产出物**：在 §3.2.M{N}.2 下输出本模块**对外暴露的真实业务接口列表**（按子领域分组）。

**接口字段硬性要求**（所有接口共同遵守，**不在 §3.2.M{N} 下重复声明这张表**）：

| 字段 | 取值约束 |
| --- | --- |
| 接口路径 | RESTful 规范 |
| 方法 | GET / POST / PUT / PATCH / DELETE |
| 请求 / 响应结构 | 引用本模块 §3.2.M{N}.3 DTO 编号 |
| 默认超时 | 见 §3.5.4 默认超时基线 |
| 默认重试 | 见 §3.5.4 |
| 幂等性约定 | 是 / 否；幂等键来源（见 §3.5.2） |
| 鉴权要求 | 角色 / 权限标签（见 §7.2） |

**接口清单输出格式**（§3.2.M{N}.2 下要使用本表头列出**真实业务接口**，**不要照抄示意行**）：

| 子领域 | 方法 | 路径 | 用途（≤ 20 字） | 请求 DTO | 响应 VO | 幂等 |
| --- | --- | --- | --- | --- | --- | --- |
| _示意 →_ `<子领域 1>` | GET | `/api/<resource>` | `<...>` | — | `<XxxListVO>` | 天然 |
| _示意 →_ `<子领域 1>` | POST | `/api/<resource>` | `<...>` | `<XxxCreateRequest>` | `<XxxVO>` | 是（biz_req_no） |
| _示意 →_ `<子领域 1>` | GET | `/api/<resource>/{id}` | `<...>` | — | `<XxxDetailVO>` | 天然 |
| _示意 →_ `<子领域 1>` | PUT | `/api/<resource>/{id}/status` | `<...>` | `<XxxStatusUpdateRequest>` | `<XxxVO>` | 是（id+version） |
| _示意 →_ `<子领域 2>` | POST | `/api/<resource>/action` | `<...>` | `<XxxActionRequest>` | `<Result>` | 是（X-Idempotency-Key） |

**填写要求**：接口数量 > 20 时按子领域再分组；用途一句话说清"做什么"，不写实现细节。

##### §3.2.M{N}.3 关键结构定义（DTO）

**该段产出物**：在 §3.2.M{N}.3 下输出本模块**特有的 DTO/VO 代码** + **关键字段定义表**。

**DTO 设计硬性要求**（所有模块共同遵守）：

- 只列出对前后端协议契约有影响的关键字段，**不展开内部模型类**
- 字段必须按"基本信息 / 业务字段 / 状态 / 时间 / 扩展"分块注释

**示意 DTO 代码骨架**（仅展示**字段分块格式**，§3.2.M{N}.3 下应替换为**真实业务 DTO**，**不要照抄本骨架**）：

```java
public class <模块>SubmitRequest {
  // ===== 必填字段 =====
  @NotBlank private String <字段1>;     // <业务含义；约束>
  @NotNull  private <类型> <字段2>;     // <业务含义>

  // ===== 可选字段 =====
  private String <字段3>;               // <业务含义；默认值>

  // ===== 扩展字段 =====
  private Map<String, Object> bizFields;  // 业务方扩展，禁止扩展业务核心语义
}

public class <模块>DetailVO {
  // ===== 基本信息 =====
  private Long id;
  private String <字段1>;               // <业务含义>

  // ===== 状态字段 =====
  private String status;                // <枚举值列表>

  // ===== 时间字段 =====
  private LocalDateTime createdAt;
  private LocalDateTime updatedAt;
}
```

**关键字段定义表输出格式**（§3.2.M{N}.3 下使用本表头列出**真实字段**，**不要照抄示意行**；用于跨模块查阅与前后端联调）：

| DTO / VO 名 | 字段名 | 类型 | 必填 | 业务含义 | 约束 / 默认值 |
| --- | --- | --- | --- | --- | --- |
| _示意 →_ `<XxxCreateRequest>` | `<字段1>` | String | 是 | `<...>` | 长度 ≤ 100 |
| _示意 →_ `<XxxCreateRequest>` | `<字段2>` | Long | 是 | `<...>` | — |
| _示意 →_ `<XxxDetailVO>` | `status` | String | 是 | 状态 | 枚举：INIT / PROCESSING / DONE |

**填写要求**：每个字段必须有中文业务含义；枚举字段必须列出全部取值（与 §1.3 术语表、§4.2 表字段定义对齐）。

##### §3.2.M{N}.4 模块逻辑时序图

**该段产出物**：在 §3.2.M{N}.4 下输出本模块**特有的时序图清单** + **关键时序图本体**（PUML / Mermaid 文本格式）。

**硬性要求**（所有模块共同遵守）：

- 覆盖模块关键路径、核心逻辑
- 模块时序图与系统时序图的模块名称等信息保持统一

**时序图清单输出格式**（§3.2.M{N}.4 下使用本表头列出**真实时序图**，**不要照抄示意行**）：

| 编号 | 标题（`<模块名> - <场景名>`） | 场景类别 | 是否包含异常分支 |
| --- | --- | --- | --- |
| _示意 →_ SQ-01 | `<...>` | 核心实体的创建 / 配置 / 发布 | 是 |
| _示意 →_ SQ-02 | `<...>` | 核心动作的执行 / 触发 / 回调 | 是 |
| _示意 →_ SQ-03 | `<...>` | 异常 / 补偿 / 回滚 | 是 |

**时序图硬性要求**（所有时序图共同遵守，**不在 §3.2.M{N} 下重复声明这张表**）：

| 要求项 | 内容 |
| --- | --- |
| 标题 | `<模块名> - <场景名>` |
| 横向参与者分层 | 前端 / 后端 / 第三方系统 / 中间件 |
| 参与者命名 | 与 §3.1.3 外部依赖 / 云组件清单、§3.1.2 系统模块图完全一致 |
| 异常分支 | 至少包含失败 / 超时 / 回滚中的 1 条 |
| 调用方式标注 | 同步 API / 异步消息 / 定时拉取 / Webhook 回调 |
| 写操作必标注 | 事务边界 / 分布式锁 Key / 幂等键传递（见 §3.5.2） |

##### §3.2.M{N}.5 关键流程逻辑

**该段产出物**：在 §3.2.M{N}.5 下输出本模块**含 Mock / 含状态机 / 跨多步异步**的关键流程伪代码 + 状态机迁移表。

**硬性要求**：

- **仅在"复杂事务、复杂状态机、跨多步异步"等复杂场景**进行展开说明；简单 CRUD 模块本段可省略，但需在 §3.2.M{N}.5 标题下显式注明"无复杂状态机/事务/跨多步异步，本段省略"

**示意伪代码骨架**（仅展示格式，§3.2.M{N}.5 下应替换为**真实业务流程**，**不要照抄本骨架**）：

```
触发：<事件名>

Step 1: <子流程 1>（同步 / 异步）
  ├── 校验：<校验项 1> → 失败时 <降级 / 返回>
  ├── 校验：<校验项 2>
  ├── [MOCK] <Mock 实现说明>
  │     输入：<...>
  │     规则：<...>
  │     输出：<...>
  │     [完整版替换点] <真实实现的接入点>
  └── 状态变更：<状态机迁移>

Step 2: <子流程 2>（异步事件）
  ├── ...
```

**状态机迁移表输出格式**（含状态机的模块必填；§3.2.M{N}.5 下使用本表头列出**真实状态迁移**，**不要照抄示意行**）：

| 起始状态 | 触发事件 | 终止状态 | 触发条件 / 校验 | 副作用 |
| --- | --- | --- | --- | --- |
| _示意 →_ INIT | submit | PROCESSING | 必填字段校验通过 | 发送事件 `<event>` |
| _示意 →_ PROCESSING | complete | DONE | 处理结果成功 | 更新业务对账记录 |
| _示意 →_ PROCESSING | fail | FAILED | 处理结果失败 | 触发补偿 |

#### 3.2.M1 `<模块1名称>`

> 按 §3.2.3 五段式模板填写。以下为占位结构，实际填写时替换为真实内容。

##### §3.2.M1.1 模块概述

`<填写：模块在业务流程中的位置 / 涉及的角色 / 涉及的内外部系统>`

##### §3.2.M1.2 接口清单

`<填写：接口清单表（按子领域分组）>`

##### §3.2.M1.3 关键结构定义（DTO）

`<填写：DTO 代码示例 + 关键字段定义表>`

##### §3.2.M1.4 模块逻辑时序图

`<填写：时序图清单 + 时序图本体>`

##### §3.2.M1.5 关键流程逻辑

`<填写：复杂流程伪代码 / 状态机迁移表；简单模块本段可省略>`

#### 3.2.M2 `<模块2名称>`

> 同 §3.2.M1，按 §3.2.3 五段式模板填写。

##### §3.2.M2.1 模块概述

`<...>`

##### §3.2.M2.2 接口清单

`<...>`

##### §3.2.M2.3 关键结构定义（DTO）

`<...>`

##### §3.2.M2.4 模块逻辑时序图

`<...>`

##### §3.2.M2.5 关键流程逻辑

`<...>`

### 3.3 核心业务对象清单

> **本节定位**：研发落地的关键索引——业务方关心 §2 业务实体，研发人员关心本节。模块开发时，公共共享的领域对象、值对象、共享 DTO / 枚举全部在此登记。

#### 3.3.1 对象定义

| 编号 | 对象名（代码类名） | 对象类型 | 包路径 | 模块归属 | 被哪些模块复用 | 实例化策略 | 序列化约定 | 持久化策略 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| O-01 | `Customer` | 聚合根 | `com.xxx.domain.customer` | `service-customer` | M1 / M2 / M3 | 工厂方法 `CustomerFactory.create()` | JSON | 落表 → `t_customer` |
| O-02 | `Order` | 聚合根 | `com.xxx.domain.order` | `service-order` | M2 / M4 | Builder | JSON | 落表 → `t_order` + `t_order_item` |
| O-03 | `OrderItem` | 实体 | `com.xxx.domain.order` | `service-order` | M2 | 由聚合根创建 | JSON | 落表 → `t_order_item` |
| O-04 | `Money` | 值对象 | `com.xxx.common.vo` | common | 全部 | new（不可变） | JSON | 嵌入字段（amount + currency） |
| O-05 | `OrderStatus` | 共享枚举 | `com.xxx.common.enums` | common | 全部 | — | String | 落表为 `status VARCHAR(30)` |
| O-06 | `CustomerExtAdapter` | 防腐层 ACL | `com.xxx.acl.customer` | `service-customer` | M1 | new + 适配 | — | 不落表，瞬时转换 |

**对象类型说明**：

| 类型 | 含义 |
| --- | --- |
| 聚合根（Aggregate Root） | 业务一致性边界；外部引用必须通过聚合根 ID；跨聚合修改禁止 |
| 实体（Entity） | 有标识、生命周期独立，但归属某个聚合根 |
| 值对象（Value Object） | 无标识、不可变；如金额、时间段、地址 |
| 共享 DTO | 跨模块传输用的数据结构，无业务行为 |
| 共享枚举 | 跨模块的状态值 / 类型值 |
| 防腐层 ACL | 对接外部系统时的转换层（与 §2.2 ACL 关系对齐） |

**填写要求**：编号 O-xx 全局唯一；对象名必须是**代码类名**（PascalCase），与实际包路径一致；防腐层对象必须显式标注，且与 §2.2 上下文映射中的 ACL 关系编号呼应。

#### 3.3.2 命名漂移登记表

> 仅登记"业务实体名 / 代码类名 / 数据表名"**三者不一致**的对象，治理跨层命名漂移。命名一致的对象无需登记。

| 业务实体（§2） | 代码类名（§3.3.1） | 数据表名（§4.2） | 漂移原因 |
| --- | --- | --- | --- |
| 用户 | `Account` | `t_account` | 避免与外部 SSO `User` 冲突 |
| 订单 | `Order` + `OrderItem` | `t_order` + `t_order_item` | 业务单一实体在代码层拆为聚合根 + 子实体 |

**填写要求**：仅命名不一致或一对多映射时登记；命名一致的对象无需出现在本表。

### 3.4 跨模块公共能力

> 仅登记**横切多个模块、以 SDK / AOP / 拦截器 / 共享 Bean 形式提供**的能力——这些能力没有独立业务边界，靠"约定接入"被多个模块共享。

**不在本节登记**（容易误填，特别说明）：

| 误填项 | 应该归到 |
| --- | --- |
| 业务模块（如 `call-service` / `ai-service` / `notify-service`），即使被多个模块调用 | §3.2 模块详细设计 + §3.5 接口契约 |
| 直接使用的云组件（如 COS / Kafka / MySQL） | §3.1.3 云组件清单 |
| 单纯的命名 / 注解 / 包结构规范 | §3.2.1 模块公共约束 |

**公共能力清单**：

| 公共能力 | 简述 | 提供方（SDK / 切面 / Bean） | 被哪些模块复用 | 接入方式 |
| --- | --- | --- | --- | --- |
| 登录鉴权 | Token 校验 / 角色 / 权限标签 | `<JwtAuthInterceptor>` + `<auth-sdk>` | 全部业务模块 | 网关拦截 + 后端拦截器 |
| 审计日志 | 关键操作留痕（合规） | `<common.aspect.AuditLogAspect>` | 全部业务模块 | AOP 切面，注解 `@AuditLog` |
| 数据上报与埋点 | 业务事件埋点 / 指标聚合 | `<TrackEventAspect>` + 异步 Stream | `<...>` | 切面，注解 `@TrackEvent` |
| 业务运行时配置 | 阈值 / 开关 / 灰度规则（业务侧） | `<RuntimeConfig Bean>`（DB + Redis 缓存） | `<...>` | Spring Bean 注入 + 配置变更监听 |

**填写要求**：
- "提供方"必须是**具体的类名 / SDK 名 / Bean 名**，不能写"系统统一提供"；
- "接入方式"必须说明研发**怎么用**（注解 / Bean 注入 / SDK 调用），便于研发评估接入成本；
- 若整张表为空（小型系统没有横切能力），整节可省略。

### 3.5 接口契约

> 本系统对外暴露的接口（含同步 API、异步消息、跨服务 RPC）在错误码、幂等、限流、超时上的全局约定。**所有接口必须遵守，不在每个接口下重复声明**。

#### 3.5.1 全局错误码体系

**错误码格式**：

| 项 | 取值 |
| --- | --- |
| 错误码格式 | `<6 位数字 / 字符串>`，例：`A010001` |
| 分段规则 | 1 位类别 + 2 位模块 + 3 位错误序号 |
| 类别取值 | `A` = 业务错误（HTTP 200，业务语义失败）/ `B` = 系统错误（HTTP 5xx）/ `C` = 客户端错误（HTTP 4xx） |

**错误响应统一结构**：

```json
{
  "code": "A010001",
  "msg": "订单不存在",
  "msgI18n": { "zh-CN": "订单不存在", "en-US": "Order not found" },
  "data": null,
  "traceId": "abc123",
  "timestamp": "2026-05-26T10:00:00.000Z"
}
```

**错误码注册表**（每模块至少登记关键业务错误，以及全局通用错误）：

| 错误码 | HTTP 状态 | 所属模块 | 含义 | 重试建议 | 用户文案 |
| --- | --- | --- | --- | --- | --- |
| A010001 | 200 | `<M1>` | `<业务语义>` | 不重试 | `<...>` |
| A010002 | 200 | `<M1>` | `<...>` | 不重试 | `<...>` |
| B999001 | 500 | 全局 | 系统内部错误 | 可重试 | 系统繁忙，请稍后再试 |
| C400001 | 400 | 全局 | 参数校验失败 | 不重试 | 参数错误 |
| C401001 | 401 | 全局 | 未登录 | 不重试 | 请重新登录 |
| C403001 | 403 | 全局 | 无权限 | 不重试 | 无权访问 |
| C429001 | 429 | 全局 | 触发限流 | 退避后重试 | 操作太频繁，请稍后再试 |

#### 3.5.2 幂等性约定

> 关键接口的幂等性约定（前端 token / 后端唯一键）。本节只回答"哪些接口需要幂等、幂等键怎么来"，**不锁定具体实现技术**。

| 接口类型 | 是否要求幂等 | 幂等键来源（建议） |
| --- | --- | --- |
| 写入类（POST 创建） | 视业务而定：涉及资金 / 通知 / 库存等"重复执行有副作用"的，**必须幂等** | Header `X-Idempotency-Key`（UUID，调用方生成） |
| 更新类（PUT / PATCH） | 必须幂等 | 资源 ID + 版本号（乐观锁） |
| 删除类（DELETE） | 天然幂等 | — |
| 查询类（GET） | 天然幂等 | — |
| 资金 / 扣款 / 发券 / 外呼 / 通知类 | **强制幂等** | 业务侧请求号（biz_req_no） |

**幂等设计需考虑的问题**（由各模块在详细设计阶段回答）：

| 问题维度 | 设计要点 |
| --- | --- |
| 幂等键的生命周期 | 保留多久（短期防抖 vs 长期对账） |
| 重复请求的语义 | 返回相同响应 vs 返回"重复提交"错误码 |
| 执行中态的处理 | 第一个请求未返回时，第二个请求如何应答（拒绝 / 等待 / 返回处理中） |
| 失败请求的可重试性 | 失败后是否允许同一幂等键重试 |
| 存储兜底 | 是否需要 DB 唯一索引作为最终防线，避免缓存丢失导致重复 |

#### 3.5.3 限流约定

| 维度 | 默认值 | 触发动作 | 调整方式 |
| --- | --- | --- | --- |
| 全局 QPS | `<10000>` | 返回 C429001 | 网关配置 |
| 单租户 QPS | `<1000>` | 返回 C429001 | 网关配置 |
| 单 IP QPS | `<100>` | 返回 C429001 + WAF 标记 | 网关配置 |
| 单用户 QPS | `<50>` | 返回 C429001 | 业务配置中心 |
| 重保接口（如登录 / 短信） | 单用户 5 次/分钟 | 锁定 15 分钟 | 业务配置中心 |

**降级策略**：

| 策略项 | 内容 |
| --- | --- |
| 前端退避 | 触发限流后，前端必须按 `Retry-After` Header 退避重试 |
| 后端记录 | 后端写入类接口同时记录到熔断队列 |
| 红线联动 | 限流红线值与 §5.5 容量水位线"红线"一致 |

#### 3.5.4 默认超时与重试基线

> 全局**单一来源**——本节定义跨模块、跨调用类型的默认超时与重试策略。各模块接口清单（§3.2.M{N}.2）通过引用本节继承默认值，仅在特殊业务场景下覆盖并显式说明理由。

| 调用类型 | 默认超时 | 默认重试 | 重试条件 |
| --- | --- | --- | --- |
| 同步 API（内部） | 3s | 0 次 | — |
| 同步 API（外部 E-xx） | 10s | 2 次（指数退避 1s / 2s） | 仅网络错误 / 5xx |
| 异步 MQ 消费 | 30s | 16 次（指数退避，封顶 1h） | 业务异常进死信 |
| 定时任务 | 5min | 3 次 | 失败告警 |

**填写要求**：超时值禁止"无限"；所有重试必须有上限；重试不能产生副作用（依赖 §3.5.2 幂等性）。

#### 3.5.5 路由设计规范

**API 路由规范**：

| 维度 | 取值 |
| --- | --- |
| 风格 | RESTful（资源化 URL） |
| 版本控制 | `/api/v1/...`（路径版本） |
| 命名 | 小写 + 中划线（kebab-case） |
| 资源命名 | 名词复数（`/api/v1/customers`） |
| 子资源 | `/api/v1/customers/{id}/orders` |
| 动作类接口 | `/api/v1/<resource>/{id}/<action>`（POST） |

**前端页面路由清单**：

> 列出**URL ↔ 页面 / 功能 ↔ 入口角色 ↔ 鉴权要求**的映射关系，是前后端联调、测试用例、权限审计的对齐基准。

| 路径 | 页面 / 功能 | 入口角色（§2.3） | 鉴权要求 | 关联模块（§3.2.M{N}） |
| --- | --- | --- | --- | --- |
| `/login` | 登录页 | 全部 | 公开 | — |
| `/workbench` | `<坐席工作台>` | `<坐席>` | 已登录 + 角色 `<agent>` | `<M1>` |
| `/manager/dashboard` | `<管理端首页>` | `<管理员 / 班组长>` | 已登录 + 角色 `<manager>` | `<M2>` |
| `/<resource>` | `<列表页>` | `<...>` | 已登录 | `<M3>` |
| `/<resource>/:id` | `<详情页>` | `<...>` | 已登录 + 数据权限校验 | `<M3>` |

**前端路由约定**（项目级一次性决策）：

| 维度 | 取值 |
| --- | --- |
| 路由模式 | History / Hash（二选一锁定） |
| 命名风格 | 小写 + 中划线（kebab-case） |
| 动态参数 | `/<resource>/:id` |
| 鉴权方式 | 路由元信息 `meta.requireAuth = true` + `meta.roles = [...]`，由全局路由守卫拦截 |
| 嵌套路由 | 列表页 → 详情页 → 子模块页 |

**填写要求**：每条路径必须显式标注鉴权要求（公开 / 已登录 / 已登录+角色 / 数据权限校验）；动态参数必须示例化为 `:id`；与 §3.2.M{N} 模块的关联必须填实，禁止"待定"。

---

## 4. 数据库设计

> **本章回答**：每个模块的状态用什么表存、表怎么建、数据怎么清理、缓存怎么用。
> **核心方法论**：**数据库按业务域组织，业务域与第 2 章 / 第 3 章保持一致**，禁止按"用户表 / 订单表 / 日志表"这种横向技术维度组织。

### 4.1 全局数据约定

> 写在所有表设计之前，作为整个项目的**数据基线**。后续每张表的字段类型、命名都必须遵循本节约定。

| 约定项 | 取值 | 说明 |
| --- | --- | --- |
| 命名规范（表名） | `t_<entity>` 业务主表；`t_<entity>_ext` 扩展表；`t_<A>_<B>_rel` N:N 关联表（按字母序） | 全项目统一前缀 `t_` |
| 命名规范（字段名） | 小写 + 下划线（snake_case） | 禁止驼峰 |
| 主键类型 | `BIGINT AUTO_INCREMENT` / `UUID` / `Snowflake` | 全项目统一，禁止混用 |
| 字符集 / 排序 | `utf8mb4 / utf8mb4_unicode_ci` | 支持 emoji 与多语言 |
| 时间字段类型 | `DATETIME(3)` UTC 存储 | 统一精度（毫秒）；统一时区（UTC） |
| 软删除字段 | `deleted_at DATETIME(3) NULL` 或 `deleted TINYINT(1) DEFAULT 0` | 二选一锁定，全项目统一 |
| 审计字段 | `created_by / created_time / updated_by / updated_time` | 命名锁定 |
| 隔离字段（多租户） | `tenant_id BIGINT NOT NULL` / `org_id BIGINT NOT NULL` | 多租户必备；查询语句强制带入 |
| 业务唯一标识 | `biz_id VARCHAR(100)` | 与外部系统对齐时使用 |
| 状态枚举 | `VARCHAR` 字符串 / `TINYINT` 数字 | 二选一并锁定；DDL 注释列出全部取值 |
| 金额字段 | `DECIMAL(N, M)` | 禁止用 `FLOAT / DOUBLE` |
| 手机号存储 | `phone VARCHAR(20)` 不带国家码前缀 | 调用三方时动态拼接 |

**填写要求**：每条约定必须给出**具体取值**，不允许"两种都行"；一旦锁定，所有表必须遵循；后续若变更，需在 §0 修订记录中明示。

### 4.2 单表设计

> 每张表必须严格按下面 5 段编写，**不得省略任何一段**。

#### 4.2.1 库表元信息

| 字段 | 取值 |
| --- | --- |
| 数据库类型 | `<MySQL / PostgreSQL / Oracle / ...>` |
| 库名 | `<schema>` |
| 表名 | `t_<xxx>` |
| 业务含义（≤ 50 字） | `<这张表存什么>` |
| 模块归属（§3.2） | `<模块名>` |
| 对应核心对象（§3.3） | `<O-xx>` |

#### 4.2.2 库表结构定义

| 列名 | 数据类型 | 可为空 | 约束 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| id | `<主键类型>` | NOT NULL | PRIMARY KEY |   | 主键 ID |
| `<隔离字段>` | BIGINT | NOT NULL |   |   | 多租户 / 多组织（如适用），查询必带 |
| `<业务字段>` | `<...>` | `<...>` | `<...>` | `<...>` | 中文含义；枚举字段必须在备注列出取值 |
| `<状态字段>` | VARCHAR(30) | NOT NULL |   | 'INIT' | 状态语义在备注写明（如：INIT / PROCESSING / DONE） |
| created_by | VARCHAR(64) | NULL |   | NULL | 创建人 |
| created_time | DATETIME(3) | NULL |   | CURRENT_TIMESTAMP | 创建时间 |
| updated_by | VARCHAR(64) | NULL |   | NULL | 更新人 |
| updated_time | DATETIME(3) | NULL |   | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| deleted | TINYINT(1) | NOT NULL |   | 0 | 删除标志（0：存在，1：已删除） |

#### 4.2.3 索引设计

| 索引名 | 索引类型 | 字段（含顺序） | 设计目的 | 关联查询场景 |
| --- | --- | --- | --- | --- |
| `PRIMARY` | 主键 | `id` | 唯一标识 | 全部按主键查询 |
| `uk_<biz>` | 唯一索引 | `tenant_id, biz_id` | 业务唯一性约束 | 防止重复录入 |
| `idx_<隔离>_<status>` | 普通索引 | `tenant_id, status` | 多租户列表查询 | `GET /api/<resource>?status=...` |
| `idx_<外键>` | 普通索引 | `<foreign_key_id>` | 关联查询加速 | `JOIN` / 反查 |
| `idx_<时间>` | 普通索引 | `created_time` | 时间范围查询 / 排序 | 按时间倒序列表 |
| `ft_<列>` | 全文索引 | `<long_text_col>` | 全文检索 | `MATCH(...) AGAINST(...)` |

**填写要求**：

| 要求项 | 内容 |
| --- | --- |
| 索引数量 | ≤ 5 条 / 表（含主键），超量需说明理由；过多索引会拖慢写入 |
| 联合索引顺序 | "最左前缀原则"：高选择性、等值查询字段在前，范围查询字段在后 |
| 关联查询场景 | 每条索引必须给出，禁止"以防万一加的索引" |
| 命名规范 | 主键 `PRIMARY`；唯一索引 `uk_<列>`；普通索引 `idx_<列>`；全文索引 `ft_<列>`；多列索引按字段顺序命名 |
| 多租户首列 | 隔离字段必须在多租户系统的所有索引中作为第一列 |
| 禁止冗余索引 | 若已有 `idx_a_b_c`，则 `idx_a` / `idx_a_b` 不再单独建 |

#### 4.2.4 DDL

```sql
CREATE TABLE `t_<entity>` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `tenant_id` BIGINT NOT NULL COMMENT '租户 ID（多租户隔离字段）',
  -- <业务字段...>
  `biz_id` VARCHAR(100) DEFAULT NULL COMMENT '外部业务唯一标识',
  `status` VARCHAR(30) NOT NULL DEFAULT 'INIT' COMMENT '状态：INIT/PROCESSING/DONE',
  `created_by` VARCHAR(64) DEFAULT NULL COMMENT '创建人',
  `created_time` DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3) COMMENT '创建时间',
  `updated_by` VARCHAR(64) DEFAULT NULL COMMENT '更新人',
  `updated_time` DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3) COMMENT '更新时间',
  `deleted` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '删除标志（0：存在，1：已删除）',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_tenant_biz` (`tenant_id`, `biz_id`) COMMENT '业务唯一性',
  KEY `idx_tenant_status` (`tenant_id`, `status`) COMMENT '多租户列表查询',
  KEY `idx_created_time` (`created_time`) COMMENT '时间范围与排序'
) COMMENT='<表的业务含义>';
```

**填写要求**：DDL 中的索引部分必须与 §4.2.3 索引设计逐条对应（数量、字段、顺序、命名一致）。

#### 4.2.5 扩展逻辑（数据清理机制）

| 清理机制 | 适用场景 | 配置 |
| --- | --- | --- |
| 无 | 永久保留 | — |
| 物理删除 | 日志、临时数据 | 按 `<时间字段>` 保留 N 天，由 `<调度任务名>` 触发 |
| 归档 | 历史数据需保留但低频访问 | 归档至 `<归档表 / 对象存储>` |
| 软删 | 业务上"删除"但需可恢复 | 仅置 `deleted = 1`，定期物理清理 |
| 分区策略 | 大表按时间 / 租户分区 | `<分区维度 + 分区数>` |
| 触发器 | 状态变更联动 | `<触发器名 + 触发条件 + 动作>` |

### 4.3 数据部署与备份

#### 4.3.1 存储清单

| 存储类型 | 用途 | 部署模式 | HA 方案 | 备份策略 | 日志 / 审计去向 |
| --- | --- | --- | --- | --- | --- |
| `<关系型 DB>` | 业务主数据 | 主从 / 集群 / 分片 | 主备同步 + 故障自动切换 | 全量 `<频率>` + 增量 `<频率>`，归档至 `<对象存储>` | `<日志服务>` |
| `<文档 DB>` | `<...>` | 副本集 / 分片 | `<...>` | `<...>` | `<...>` |
| `<搜索引擎>` | 全文检索 / 日志 | 多节点集群 | 副本数 ≥ 1 | `<快照频率>` | `<...>` |
| `<分析型 DB>` | 业务指标计算 | `<...>` | `<...>` | `<...>` | `<...>` |
| `<缓存>` | 缓存 / 会话 | 主从 / 集群 / 哨兵 | 故障自动切换 | 是否需要持久化备份 | `<...>` |
| `<对象存储>` | 文件 / 备份 | 多 AZ | 内置冗余 | 跨区域复制（如适用） | `<...>` |

#### 4.3.2 备份与恢复

| 维度 | 取值 |
| --- | --- |
| 备份方式 | 全量 + 增量；冷备 + 热备 |
| 备份位置 | 与生产环境**物理 / 可用区隔离**的对象存储 |
| 保留策略 | 全量保留 `<30 天 / 90 天 / 长期>`；增量保留 `<...>`；不同存储分别给出 |
| RPO（数据丢失容忍） | ≤ `<...>` 分钟（与 §5.3 SLA 自洽） |
| RTO（恢复时间目标） | ≤ `<...>` 分钟 |
| 恢复演练 | 频率 `<季度 / 半年>`；负责人 `<...>` |

**填写要求**：每类持久化存储均出现在 §4.3.1；主库具备 HA；备份与生产隔离；RPO/RTO 有数字且与 §5.3 SLA 自洽。

### 4.4 缓存与 Key 规范

> 凡是用到 Redis / Memcached 等缓存组件的系统**必填本节**。缓存 Key 是"分布式系统的隐形数据库"，命名混乱会导致清理困难、串数据、TTL 失控。

#### 4.4.1 Key 命名规范

| 规则项 | 取值 |
| --- | --- |
| 统一前缀 | 每个业务领域使用统一前缀，如 `auth:` / `lead:` / `call:` |
| 多级分隔符 | 用冒号 `:` 分层，禁止用下划线、点号 |
| 变量占位 | 用 `{}` 表示变量部分，如 `auth:login:fail:user:{username}` |
| 环境隔离 | 禁止用 Key 前缀做环境隔离（应使用不同 Redis 实例 / DB） |

#### 4.4.2 缓存策略与一致性

> 凡涉及"DB + 缓存"双写的场景，本节必填；纯缓存（如限流计数 / 分布式锁 / 幂等键）可省略。

| 维度 | 在设计阶段需要明确的内容 |
| --- | --- |
| 读写策略 | Cache-Aside / Read-Through / Write-Through / Write-Behind 等，二选一并说明理由 |
| 一致性级别 | 强一致 / 最终一致 / 容忍短暂不一致；与 §2.5.1 质量优先级冲突决策对齐 |
| 更新顺序 | 先更新 DB 再失效缓存 / 先失效缓存再更新 DB；并发场景兜底（延迟双删 / 版本号 / 订阅 binlog 等） |
| 失效防护 | 是否需应对缓存击穿（热点 Key 重建）/ 穿透（空值 / 不存在的查询）/ 雪崩（大批 Key 同时过期） |
| 降级策略 | 缓存不可用时的兜底（直查 DB / 限流保护 DB / 返回降级数据） |

**填写要求**：每条业务缓存 Key 在 §4.4.3 备注列引用本节中的策略选择；不允许"用 Redis"这类没有策略的笼统描述。

#### 4.4.3 Key 清单

| Key 模式 | 类型 | TTL | 用途 | 写入位置 | 删除时机 |
| --- | --- | --- | --- | --- | --- |
| `<域>:<实体>:{<id>}` | String | `<...>` | `<一句话用途>` | `<服务名.方法名>` | `<TTL 自动 / 业务主动 / 定时清理>` |
| `<域>:<事件>:{<key>}` | List / Stream | `<...>` | `<...>` | `<...>` | `<...>` |
| `<域>:<聚合>:{<id>}` | Hash | `<...>` | `<...>` | `<...>` | `<...>` |
| `<域>:lock:{<resource>}` | String | `<...>` | 分布式锁 | `<...>` | 持有方主动 DEL |
| `<域>:rl:{<dim>}:{<id>}` | String (counter) | `<分钟 / 小时>` | 限流计数 | 拦截器 | TTL 自动 |
| `<域>:idem:{<uuid>}` | String | `<24h>` | 幂等去重 | 接口入口 | TTL 自动 |

**填写要求**：每条 Key 必须给出 **TTL 数字**（不能省略）；用途列必须明确"为什么用缓存而不直接查数据库"（性能 / 跨进程通信 / 限流 / 锁）；写入位置写到方法级。

### 4.5 数据迁移与兼容性

> 存量数据如何迁移到新系统、新老系统并行期如何兼容、出问题如何回滚。
> **适用范围**：替换 / 重构既有系统**强制必填**；全新系统可省略本节，但需在 §0 修订记录中注明"无存量数据迁移"。

#### 4.5.1 迁移范围与数据源

| 数据域 | 源系统 | 源存储 | 数据量 | 增量速度 | 目标库表（§4.2） |
| --- | --- | --- | --- | --- | --- |
| `<域1>` | `<旧系统>` | `<MySQL 5.7>` | `<5000 万行 / 200GB>` | `<10 万 / 天>` | `t_<entity_a>` |
| `<域2>` | `<外部 SaaS>` | `<API 导出>` | `<100 万行>` | 一次性 | `t_<entity_b>` |
| `<域3>` | `<旧 NoSQL>` | `<MongoDB 4.x>` | `<...>` | `<...>` | `t_<entity_c>` |

#### 4.5.2 迁移方案选型

| 方案 | 适用场景 | 是否选用 | 说明 |
| --- | --- | --- | --- |
| 停机全量迁移 | 数据量小（< 100GB）、可接受停机窗口 | 是 / 否 | `<...>` |
| 存量 + 增量（双写） | 数据量大、不可停机 | 是 / 否 | `<...>` |
| 灰度切流 | 风险敏感、需逐步验证 | 是 / 否 | `<...>` |
| CDC（Binlog 同步） | 异构数据库、实时同步要求 | 是 / 否 | `<...>` |

**选型理由**：`<一句话说明为什么选 A 不选 B>`

#### 4.5.3 迁移分阶段计划

| 阶段 | 阶段名称 | 进入条件 | 退出条件 | 回滚预案 | Owner |
| --- | --- | --- | --- | --- | --- |
| T0 | 方案评审 + 工具准备 | 评审通过 | 工具就绪（DataX / Canal / Flink CDC / 自研脚本） | — | `@<架构>` |
| T1 | 存量全量同步（一次性，离线） | 工具就绪 + 演练通过 | 全量同步完成 + 校验通过 | 重新拉取 | `@<DBA>` |
| T2 | 增量双写（新旧系统同时写） | T1 完成 | 双写差异 < 阈值持续 24h | 关闭新系统写 | `@<研发>` |
| T3 | 读流量灰度（1% → 10% → 50% → 100%） | T2 稳定 7 天 | 灰度 100% 且错误率达标 | 切回旧系统读 | `@<研发>` |
| T4 | 写流量切换 | T3 完成 | 写切换后 24h 稳定 | 反向同步 + 切回旧系统 | `@<研发>` |
| T5 | 老系统旁路 / 下线 | T4 稳定 N 天 | — | — | `@<DBA>` |

#### 4.5.4 数据校验

| 校验类型 | 方式 | 阈值 |
| --- | --- | --- |
| 行数对比 | `COUNT(*)` 抽样 / 全量 | 偏差 = 0 |
| 内容对比 | 抽样 1‰ + 关键字段 checksum | 不一致率 < 0.001% |
| 业务对账 | 关键聚合指标对比（金额、订单数） | 偏差 < 0.01% |
| 实时差异告警 | Binlog 双写差异采集 | 差异 > 阈值即告警 |

**填写要求**：每个数据域必须明确"哪几类校验必做 + 阈值数字"，禁止"人工抽查"等不可量化方式。

#### 4.5.5 回滚预案

> 迁移失败时的回滚预案。

| 阶段 | 回滚动作 | RTO | 数据丢失风险 |
| --- | --- | --- | --- |
| T2 双写 | 关闭新系统写入，旧系统继续承载 | < 5 min | 0 |
| T3 灰度读 | 切回旧系统读 | < 2 min | 0 |
| T4 写切换 | 反向同步新→旧，切回旧系统 | < 30 min | 切换窗口内的写入需补偿 |

---

## 5. 部署架构

> **本章定位**：本章只承载**对开发产生约束的部署结论**——开发要据此设计容错、降级、限流、分片、连接池、外部访问。具体节点规格、CICD 流水线阶段、回滚 SOP 等施工细节见《系统部署文档》。

### 5.1 环境与集群划分

| 环境名称 | 环境定位 | 集群隔离 | 集群规格 |
| --- | --- | --- | --- |
| dev | 开发自测 | 与其他系统共用 | `<最小规格>` |
| int | 联调 | 共用 / 独立 | `<...>` |
| uat | 预发 / 验收 | 独立 | `<接近生产>` |
| prod | 生产 | 独立 | `<生产规格>` |

**填写要求**：仅锁定"环境数量 + 是否独立集群"两项，规格 / 节点数等细节见《系统部署文档》。

**配置策略**：

| 配置层级 | 存放位置 | 适用内容 | 变更生效 | 对开发的约束 |
| --- | --- | --- | --- | --- |
| L1 代码内 | 代码仓库 | 默认值、枚举、不会变的常量 | 重新部署 | 禁止写入：环境差异、密码、地址 |
| L2 配置中心 | `<Nacos / Apollo>` | 业务开关、限流阈值、灰度规则、降级开关 | 热更新（监听 + 回调） | 必须实现配置变更回调；变更必须幂等 |
| L3 环境变量 / ConfigMap | K8s ConfigMap | 环境差异（DB 地址 / MQ 接入点 / 外部域名） | 重启 Pod | 启动失败 fail-fast，禁止跑默认值 |
| L4 Secret | KMS / Secret Manager | 密码、AKSK、Token、证书私钥 | 动态拉取 / 重启 | 禁止落日志、禁止入代码、内存中不缓存到生命周期外 |

### 5.2 运行时高可用与弹性

#### 5.2.1 负载均衡

| 维度 | 取值 |
| --- | --- |
| 类型 | 4 层（L4）/ 7 层（L7） |
| 健康检查路径 | `/healthz`（Liveness）/ `/readyz`（Readiness） |
| 健康检查频率 | `<10s>` |
| 失败阈值 | 连续 `<3 次>` 失败标记不健康 |
| 恢复阈值 | 连续 `<2 次>` 成功标记健康 |
| 会话保持 | 是 / 否；策略（IP Hash / Cookie） |

#### 5.2.2 弹性伸缩

| 维度 | 取值 |
| --- | --- |
| 触发指标 | CPU / QPS / 队列长度 |
| 扩容阈值 | CPU > 70% 持续 5min |
| 缩容阈值 | CPU < 30% 持续 10min |
| 副本区间 | min `<2>` / max `<20>` |
| 冷却时间 | 扩容 `<60s>` / 缩容 `<5min>` |

#### 5.2.3 容灾级别

| 维度 | 取值 |
| --- | --- |
| 容灾级别 | 单 AZ / 多 AZ / 多 Region |
| 故障切换 RTO | ≤ `<分钟>`（与 §5.3 SLA 自洽） |
| 跨 Region 数据同步策略 | `<同步 / 异步>`；延迟 `<...>` |

#### 5.2.4 可用性来源拆分

| 可用性来源 | 范围 | 责任方 |
| --- | --- | --- |
| 云厂商 SLA 兜底 | IaaS（计算 / 网络 / 存储）/ PaaS（DB / Redis / MQ 托管） | 云厂商 |
| 本系统自身保障 | 业务逻辑容错 / 重试 / 熔断 / 降级 / 兜底数据 | 研发团队 |
| 强依赖外部（E-xx）故障策略 | 熔断 / 降级 / 排队 / 拒绝服务 | 研发团队 |

**对开发的约束**：

| 契约项 | 本系统取值 | 开发实现要求 |
| --- | --- | --- |
| 无状态化 | `<全部无状态 / Session 外置到 Redis / 有状态服务清单>` | 无状态：Pod 内禁止本地落文件；有状态：必须显式标注 |
| 优雅停机 | SIGTERM → 停止接入新流量 → 处理在飞请求 → 退出；超时上限 `<30s>` | 实现 `preStop` Hook；HTTP/gRPC server 实现 shutdown |
| Liveness | `/healthz`，仅判进程存活 | 失败 → 重启 Pod |
| Readiness | `/readyz`，判依赖（DB/Redis）就绪 | 失败 → 摘流不杀进程 |
| 幂等性范围 | 全部写接口必须幂等（见 §3.5.2）；MQ 消费者必须幂等 | 幂等键来源、存储、有效期在模块详细设计中说明 |
| 超时与重试基线 | 默认值见 §3.5.4 | 跨服务调用必须显式设置超时；任何重试必须配合幂等键 |

### 5.3 服务级别（SLA）

| SLA 指标项 | 指标定义 | 目标值 | 度量方式 |
| --- | --- | --- | --- |
| 系统开放时间 | 对外提供服务的时间窗口 | `<7×24 / 9:00–21:00 / 7×8h>` | 业务侧约定 |
| 系统可用性 | 月度可用时长 / 月度总时长 | ≥ `<99.5% / 99.9% / 99.95%>` | 监控统计 |
| 单接口分位响应时延 | 端到端响应时间 | P95 ≤ `<...>` ms；P99 ≤ `<...>` ms | APM 采样 |
| 故障恢复目标（RTO、RPO） | 故障到恢复的时间 / 数据丢失容忍 | RTO ≤ `<分钟>`；RPO ≤ `<分钟>` | 演练验证 |
| 计划内停机窗口 | 升级 / 维护窗口 | `<频率 + 时长 + 通知提前期>` | 公告 |
| 不可用界定 | 何为"不可用" | `<错误率 > X% 持续 N 分钟>` 或 `<连续 N 次健康检查失败>` | 告警规则 |

**判定合格**：可用性 / RTO / RPO / P99 四项必须有数字，且与 §4.3 备份恢复、§5.2 容灾级别自洽。

### 5.4 部署拓扑图

#### 5.4.1 物理拓扑图

> 完整覆盖接入网络拓扑、Region 地域划分、集群划分、节点划分等物理部署细节。

#### 5.4.2 拓扑要素清单

| 要素 | 取值 |
| --- | --- |
| 跨 Region 部署 | 是 / 否；Region 数量 `<...>` |
| 跨 AZ 部署 | 是 / 否；AZ 数量 `<...>` |
| 流量入口路径 | `<DNS → CDN → DDoS → WAF → LB → 网关 → 业务>` |
| 内部调用路径 | `<服务发现方式 + 协议>` |
| 数据库访问路径 | `<连接池 + 主从切换>` |

#### 5.4.3 故障域划分

| 故障域 | 影响范围 | 容错机制 | 预期 RTO |
| --- | --- | --- | --- |
| 单 Pod | 单实例 | K8s 自动重建 | < 30s（瞬时无影响） |
| 单 Node | 该 Node 上的全部 Pod | K8s 调度到其他 Node | < 1min |
| 单 AZ | 该 AZ 全部资源 | 跨 AZ 部署，自动切换 | ≤ 5min |
| 单 Region | 全部主资源 | 灾备 Region 切换 | < 1h |

### 5.5 容量规划

#### 5.5.1 业务量预估

| 业务指标 | 当前值 | MVP 阶段值 | 生产阶段值 | 备注 |
| --- | --- | --- | --- | --- |
| 注册用户数 | `<...>` | `<...>` | `<...>` | PRD / 业务方 |
| DAU | `<...>` | `<...>` | `<...>` | 业务方 |
| 峰值 QPS | `<...>` | `<...>` | `<...>` | 推算（DAU × 人均 PV × 峰值系数） |
| 数据写入量 / 天 | `<...>` | `<...>` | `<...>` | 推算 |
| 数据存量 | `<...>` | `<...>` | `<...>` | 推算（增量累加） |

#### 5.5.2 资源容量推算

| 资源 | 推算公式 | 当前配置 | 生产阶段配置 |
| --- | --- | --- | --- |
| 应用实例数 | 峰值 QPS / 单实例 QPS × 冗余系数 1.5 | `<...>` | `<...>` |
| 带宽 | 峰值 QPS × 平均大小 × 1.3 | `<...>` | `<...>` |
| MySQL 主库 | (峰值写 TPS × 平均 SQL 数) → IOPS | 2C8G + 200GB | 4C16G + 1TB |
| MySQL 从库数 | 读 QPS / 单实例承载 | 2 个 | 4 个 |
| Redis | 内存 = 单 Key 大小 × 数量 × 1.3 | `<...>` | `<...>` |
| Kafka | Topic 分区数 = 峰值 TPS / 单分区承载 | `<...>` | `<...>` |
| 对象存储 | 日增 × 保留天数 × 副本系数 | `<...>` | `<...>` |

#### 5.5.3 扩容触发与路径

| 资源 | 扩容触发指标 | 扩容方式 | 扩容耗时 |
| --- | --- | --- | --- |
| 应用 Pod | CPU > 70% 持续 5min | HPA 自动扩 | < 1min |
| MySQL | CPU > 80% / IOPS > 80% | 升配（短期）/ 读写分离（中期）/ 分库分表（长期） | 升配 < 30min |
| Redis | 内存 > 70% | 升配 / 集群扩节点 | < 30min |
| Kafka | 分区堆积 > 100 万 | 增加分区 + 消费者 | < 10min |
| 对象存储 | 容量 > 80% | 自动扩展 / 手动扩容 | < 10min |

#### 5.5.4 容量水位线

| 水位 | 阈值 | 动作 | 对开发的约束 |
| --- | --- | --- | --- |
| 健康水位 | < 60% | 无动作 | — |
| 预警水位 | 60-80% | 告警，启动扩容评审 | — |
| 危险水位 | > 80% | 自动扩容（如支持）或紧急扩容 | — |
| 红线 | > 95% | 触发限流 / 降级（与 §3.5.3 联动） | 限流红线值需与本表一致；降级开关在配置中心预埋（§5.1 L2） |

**判定合格**：业务量预估 1-3 年区间齐全，含来源；红线水位 → §3.5.3 限流默认值 → §8.4 告警规则数值一致。

---

## 6. 网络架构

> **本章定位**：本章只承载**对开发产生约束的网络结论**——开发要据此处理 HTTPS、限流位置、外部访问出口、服务发现与服务间通信。VPC / 子网 / CIDR 划分等运维施工细节见《系统部署文档》。

### 6.1 网络环境规划

| 网络环境 | 用途 | 说明 / 访问策略 |
| --- | --- | --- |
| DMZ | 公网入口缓冲区，部署公网 LB / 反向代理 | 仅允许从公网入站；不能直接访问 INT / PROD |
| INT | 内部互联网络，部署应用与中间件 | 仅允许从 DMZ 入站；出站受 NAT 控制 |
| UAT | 预发环境，独立网段 | 与 PROD 隔离；可访问 PROD 镜像 / 备份 |
| PROD | 生产环境，独立网段 | 严格的入站 / 出站 ACL；最小化暴露 |
| DEV | 开发环境，开发 VPC | 与 PROD 完全隔离；不允许访问生产数据 |

### 6.2 流量入口与南北向流量

#### 6.2.1 入口流量链路

| 组件 | 作用 | 部署位置 |
| --- | --- | --- |
| DNS | 域名解析 / 智能调度 | 公网 |
| CDN | 静态资源缓存 / 加速 | 公网 |
| 抗 DDoS | 流量清洗 | 公网入口 |
| WAF | 应用层防护（SQL 注入 / XSS / CC） | 公网入口 |
| 公网 LB（4 层 / 7 层） | TLS 终结 / 流量分发 | DMZ |
| API 网关 / Ingress | 路由 / 鉴权 / 限流 | 应用子网入口 |
| 业务服务 | 业务逻辑 | 应用子网 |

**入口决策（对开发的约束）**：

| 决策项 | 取值 | 对开发的约束 |
| --- | --- | --- |
| TLS 终结点 | `<CDN / 公网 LB / API 网关 / 业务服务>` | 终结点之后是否需应用层重新加密；业务侧是否需解析 X-Forwarded-* 头 |
| 限流位置 | `<API 网关统一拦截 / 业务自实现 / 双层>` | 业务自实现限流时需对接 §3.5.3 限流红线 |
| 鉴权位置 | `<API 网关统一鉴权 / 业务自鉴权 / 双层>` | 与 §7.2.1 用户认证一致 |
| 真实客户端 IP 获取 | `<X-Forwarded-For / X-Real-IP / Proxy Protocol>` | 业务获取客户端 IP 必须用本字段，不能直接用 socket 远端 IP |

#### 6.2.2 出口流量

| 决策项 | 取值 | 对开发的约束 |
| --- | --- | --- |
| 出口方式 | `<NAT 网关 / 出口代理 / VPC Endpoint>` | 决定外部 SDK 是否能直连；私有化场景需明示 |
| 是否需要固定出口 IP | 是 / 否 | 是：业务必须只能从指定环境访问 §3.1.3 中的 E-xx；否则触发对方白名单拦截 |
| 出口白名单 | 全部 §3.1.3 E-xx 的访问域名 / 地址（条目级表达） | 业务对接 E-xx 时必须使用白名单内的域名；新增 E-xx 必须先登记 |
| 跨地域 / 跨云出口 | `<专线 / VPN / 公网>` | 不同链路对应不同延迟 / 安全等级 |

### 6.3 内部互联（东西向流量）

| 决策项 | 取值 | 对开发的约束 |
| --- | --- | --- |
| 服务发现 | `<K8s Service / 注册中心（Consul / Nacos / 北极星）>` | 服务调用方必须使用统一的 SDK，禁止硬编码 IP / 直连 Pod IP |
| 服务间通信协议 | `<HTTP / gRPC / MQ>`（按调用类型选用） | 同步重链路用 gRPC；解耦异步用 MQ；与 §3.5.4 超时重试基线对齐 |
| 内部 mTLS | 启用 / 不启用 | 启用时调用方需配置客户端证书；证书生命周期与轮换由部署文档管理 |
| 跨 VPC 互联 | `<PrivateLink>` | 跨 VPC 调用走私网，禁止公网回环 |
| 跨账号互联 | `<CCN / PrivateLink>` | 多账号场景的私网互通方式 |
| 跨地域互联 | `<CCN / 专线>` | 多地域延迟基线，业务需考虑跨地域调用的容忍度 |
| 跨服务调用幂等性 | 必须遵循 §3.5.2 幂等键传递 | 跨服务调用必须在请求 Header 透传幂等键与 traceId（§8.3） |

**判定合格**：§6.1 / §6.2 / §6.3 所有决策项 100% 已锁定；TLS 终结点、限流位置、鉴权位置三项与 §3.5 / §7 数值与位置一致；服务发现与服务间通信协议已锁定。

---

## 7. 安全设计

> **本章回答**：本系统在安全维度做了哪些选择、对应到哪些安全产品 / 能力域、关键机制如何落地。
> 若团队同时编写独立的《系统安全设计》文档，本章承载"机制选择 + 关键参数基线"，更深的细节（完整威胁模型 STRIDE、OWASP 防御对照、IAM 策略片段等）整体引用至独立文档。

### 7.1 架构安全全景

| 能力域 | 选用产品 / 机制 | 作用范围 | 是否启用 |
| --- | --- | --- | --- |
| 抗 DDoS | `<产品>` | 公网入口 | 是 / 否（不适用：`<原因>`） |
| WAF | `<产品>` | Web 应用层 | 是 / 否 |
| 防火墙 / 安全组 | `<产品>` | VPC / 子网 / 主机 三级 | 是 / 否 |
| 主机安全 | `<产品>` | 全部主机 | 是 / 否 |
| 容器安全 | `<产品>` | 全部容器 | 是 / 否 |
| 数据安全审计 | `<产品>` | 关键数据库 | 是 / 否 |
| 密钥管理（KMS） | `<产品>` | 敏感数据加密 / AKSK 保护 | 是 / 否 |
| 安全运营中心（SOC） | `<产品>` | 安全事件监控与响应 | 是 / 否 |
| 堡垒机 | `<产品>` | 运维通道审计 | 是 / 否 |

**填写要求**：每个能力域必须出现；不启用则填"不适用 / 由 `<上游系统>` 统一保障"，**禁止留空**。

### 7.2 业务安全架构

#### 7.2.1 用户认证

| 维度 | 取值 |
| --- | --- |
| 账号体系 | 自建 / 基于 `<SSO / IDP>` / 三方互联（微信 / 企微 / OAuth2） |
| 登录方式 | 账号密码 / 验证码 / SSO / 扫码 / 生物识别 |
| 多因素认证（MFA） | 是否启用、覆盖范围（管理端 / 工作台 / 全部） |
| 密码强度 | 最小长度 `<12>`；复杂度（大小写 + 数字 + 符号） |
| 登录失败锁定 | 连续失败 `<5>` 次锁定 `<15 分钟>` |
| Token 有效期 | 访问 Token `<2h>`；刷新 Token `<7d>` |
| 会话管理 | 单点 / 多点登录策略；Session / JWT 选择；注销与续签机制 |

#### 7.2.2 数据安全

| 维度 | 取值 |
| --- | --- |
| 数据分级 | 是否做分级（如 L1~L4）；分级标准与对应处理策略 |
| 传输加密 | TLS 是否强制；内部服务间通信是否启用 mTLS |
| 存储加密 | 是否启用 TDE / SSE / CSE；加密范围（全库 / 敏感字段） |
| 敏感字段清单 | `<手机号 / 身份证号 / 银行卡号 / 地址>` 对应的脱敏 / 加密策略 |
| 数据导出与共享 | 导出审批流；共享数据的脱敏要求 |

**敏感字段处理策略表**：

| 字段 | 分级 | 存储策略 | 展示策略（脱敏） | 导出策略 |
| --- | --- | --- | --- | --- |
| 手机号 | L3 | 加密存储（AES-256） | 中间 4 位 `*` 屏蔽 | 需审批 + 脱敏 |
| 身份证号 | L4 | 加密存储 | 仅展示后 4 位 | 禁止导出 |
| 银行卡号 | L4 | 加密存储 | 仅展示后 4 位 | 禁止导出 |
| 地址 | L2 | 明文存储 | 完整展示 | 不限制 |

#### 7.2.3 密钥与凭证

| 维度 | 取值 |
| --- | --- |
| 存储方式 | KMS / Secrets Manager / 配置中心加密 |
| 下发方式 | IAM 角色 / 临时凭证 / 长期 AKSK，**优先采用临时凭证** |
| 轮换策略 | 是否定期轮换；轮换周期基线 `<90 天>`；轮换是否需要业务停机 |
| 红线 | 禁止入库 / 入代码 / 入配置文件明文；禁止跨环境共用密钥 |

#### 7.2.4 审计日志

| 维度 | 取值 |
| --- | --- |
| 启用的日志类型 | 云资源 / 业务操作 / 数据库 / 堡垒机 / WAF |
| 审计内容 | 必须包含"谁、何时、对什么资源、做了什么、结果如何、来源 IP" |
| 不可篡改保障 | 独立账号 / 独立桶存储；与业务存储物理隔离 |
| 保留期 | 合规要求下的最低保留时长 `<≥ 1 年>`，与 §8.2 一致 |

#### 7.2.5 访问控制

| 维度 | 取值 |
| --- | --- |
| 权限模型 | RBAC0 / RBAC1 / RBAC2 / ABAC，二选一并说明理由 |
| 多租户隔离 | 物理隔离 / 逻辑隔离（隔离字段见 §4.1） |
| 越权防护 | 列表与详情接口都必须校验数据归属；横向越权 / 纵向越权的防护策略 |
| 接口级权限 | 与 §3.5 调用契约的对齐（每个接口的角色 / 权限标签） |

**判定合格**：
- §7.1 安全能力全景表无空白；
- §7.2 五项维度均给出"本系统的选择 + 关键基线"，不允许"待定"或"详见安全文档"代替正文。

---

## 8. 可观测设计

> **本章回答**：系统跑起来之后，怎么知道它健康、出问题怎么定位、用户体验是否达标。
> **三大支柱**：Metrics（指标）/ Logs（日志）/ Traces（链路追踪）。

### 8.1 Metrics

#### 8.1.1 指标分层

| 指标层 | 示例 | 工具 |
| --- | --- | --- |
| 业务指标 | 订单数 / 转化率 / GMV | 系统监控 & 自定义埋点 |
| 应用指标 | RED：Rate / Errors / Duration | 系统监控 & 自定义埋点 |
| 中间件指标 | DB QPS / 慢查询 / Redis 命中率 / Kafka Lag | 系统监控 |
| 资源指标 | USE：Utilization / Saturation / Errors（CPU / 内存 / 磁盘 / 网络） | 系统监控 |

#### 8.1.2 关键指标 Dashboard 清单

| Dashboard 名 | 受众 | 核心指标 | 刷新频率 |
| --- | --- | --- | --- |
| 业务大盘 | 产品 / 业务 | 核心业务指标、关键漏斗 | 1min |
| 服务健康大盘 | 研发 / SRE | RED（Rate / Errors / Duration） | 30s |
| 中间件大盘 | SRE / DBA | DB / Redis / Kafka 全维度 | 30s |
| 容量大盘 | SRE | §5.5 全部资源水位 | 1min |
| 业务预警大盘 | On-call | 当前活跃告警 | 实时 |

### 8.2 Logs

#### 8.2.1 日志分级与去向

| 日志类型 | 级别 | 保存时间 | 日志去向 |
| --- | --- | --- | --- |
| 应用日志（业务） | INFO+ | 30 天热 + 180 天冷 | 日志组件（结构化 JSON） |
| 应用日志（异常） | ERROR+ | 90 天热 + 1 年冷 | 日志组件 + 实时告警 |
| 接入日志（Nginx / 网关） | INFO+ | 30 天 | 日志组件 |
| 审计日志 | INFO+ | ≥ 1 年（合规要求由安全设计方案主导） | 独立账号独立桶（防篡改） |
| 慢查询日志 | — | 30 天 | 日志组件 |

#### 8.2.2 结构化日志规范

```json
{
  "timestamp": "2026-05-26T10:00:00.000Z",
  "level": "INFO",
  "service": "<service-name>",
  "instance": "<pod-name>",
  "traceId": "<W3C TraceContext>",
  "spanId": "<...>",
  "tenantId": "<...>",
  "userId": "<...>",
  "module": "<module-name>",
  "event": "<event-name>",
  "msg": "<...>",
  "extra": { }
}
```

**硬性要求**：

| 要求项 | 内容 |
| --- | --- |
| 必含字段 | 所有日志必须含 `traceId` + `tenantId` |
| 敏感字段 | 禁止打印敏感字段明文（与 §7.2.2 数据安全联动） |
| ERROR 级别 | 必须含完整堆栈 + 业务上下文（订单号 / 用户 ID 等） |

### 8.3 Traces

| 项 | 取值 |
| --- | --- |
| 协议标准 | OpenTelemetry / W3C TraceContext |
| 采样策略 | 默认 1% 采样；错误请求 100% 采样；慢请求（>1s）100% 采样 |
| 透传方式 | HTTP Header `traceparent` / `tracestate`；MQ 消息属性透传 |
| 关键 Span 命名 | `<service>.<method>` / `db.<operation>` / `mq.<topic>.<send\|consume>` |
| 跨外部系统 | 调用 E-xx 时透传 `traceId`（若对方支持），并记录 `peer.service` |

**与时序图的关联**：§3.2.5 模块内时序图要求标注 traceId 透传节点，本节是其落地规范。

### 8.4 告警体系

#### 8.4.1 告警分级

| 级别 | 适用场景 | 响应时间 | 通知方式 |
| --- | --- | --- | --- |
| P0 紧急 | 业务中断 / 关键指标严重劣化 | ≤ 5 min | 电话 + 短信 + IM |
| P1 高 | 部分功能受影响 | ≤ 15 min | 短信 + IM |
| P2 中 | 单点异常但有冗余 | ≤ 1 h | IM |
| P3 低 | 趋势性预警 | 工作时间 | IM / 邮件 |

#### 8.4.2 告警规则编排

| 编号 | 级别 | 触发条件 | 维度 | Owner | Runbook |
| --- | --- | --- | --- | --- | --- |
| AL-01 | P0 | 5xx 错误率 > 1% 持续 5min | 接口可用性 | `@<研发>` | `<wiki/runbook/AL-01>` |
| AL-02 | P0 | DB 主库不可访问 | 中间件健康 | `@<DBA>` | `<...>` |
| AL-03 | P1 | P99 > 500ms 持续 5min | 接口延迟 | `@<研发>` | `<...>` |
| AL-04 | P2 | Kafka Lag > 10 万 | 队列堆积 | `@<研发>` | `<...>` |
| AL-05 | P2 | CPU > 80% 持续 10min | 资源水位 | `@<运维>` | `<...>` |

**判定合格**：
- 关键指标至少覆盖业务 / 应用 / 中间件 / 资源四层；
- 关键告警均能映射到具体指标；
- 全部 P0 / P1 告警有 Owner；
- 关键 Dashboard ≥ 5 个；
- 日志含 traceId + tenantId；
- 采样策略覆盖错误 / 慢请求兜底；
- §5.3 SLA、§2.5.1 质量目标与本章告警阈值数值自洽。
