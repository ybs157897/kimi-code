# AICoding 架构设计 · 资料摘要

> 本文档做一件事：**精读主理人转交的全部原始资料，逐份、逐章节做出摘要**——后面任何人拿到这份摘要，都能通过章节号快速定位回原始文件的对应位置。

> 上游输入：主理人转交的全部原始资料（docx / pdf / pptx / xlsx）；
> 产出者：`knowledge-ingest-engineer`（知识摄入工程师 - 闻资料），经 G1 校验与人工审核通过后交付。

---

## 模版使用说明（必读）

> ⚠️ **本文件是「模版」而非真实摘要**。所有文字、表格中出现的业务内容**仅作为示意**，并不代表当前项目的真实业务范围。

| 标记 | 含义 | 处理方式 |
| --- | --- | --- |
| `<...>` | 占位符 | **必须**替换或删除 |
| `示例：` / `例：` 前缀 | 填写参考 | **必须**替换为真实内容或整行删除 |
| `YYYY-MM-DD` / `<n>` / `<x>` | 待填日期 / 数字 | **必须**替换为真实数值 |
| `[待补充]` | 事实缺口 | 最终**必须**替换 |

**填写纪律**：
1. 定稿前全文不得残留 `<...>`、`示例：`、`YYYY-MM-DD`、`[待补充]`。
2. 表格示例行可覆盖，但**表头不可删除**。
3. 附录为元信息，不需按业务替换。

---

## 0. 元信息

```yaml
标题: <项目名> - 资料摘要 v<MAJOR.MINOR>
版本: v0.1
状态: Draft
创建日期: YYYY-MM-DD
整理人: <姓名>
审核人:
  - <姓名 / 主理人>

原始资料清单:
  - <资料A路径>: <一句话说明>
  - <资料B路径>: <一句话说明>
```

| 版本 | 日期 | 作者 | 变更内容 |
| --- | --- | --- | --- |
| v0.1 | YYYY-MM-DD | `<整理人>` | 初稿 |

---

## 1. 资料清单

> 列出全部原始资料，每份标注解析状态。解析失败或跳过的必须注明原因。

| 编号 | 文件名 | 类型 | 来源 | 解析状态 | 说明 |
| --- | --- | --- | --- | --- | --- |
| D1 | `<例：产品需求文档_v2.3.docx>` | docx | `<产品经理>` | 已解析 | — |
| D2 | `<例：竞品分析.pdf>` | pdf | `<业务负责人>` | 已解析 | — |
| D3 | `<例：技术方案汇报.pptx>` | pptx | `<技术团队>` | 已解析 | — |
| D4 | `<例：数据字典.xlsx>` | xlsx | `<数据团队>` | 解析失败 | `<格式损坏，已申请重新导出>` |
| D5 | `<例：接口手册_v1.0.pdf>` | pdf | `<第三方>` | 待补充 | `<等待对方提供更新版>` |

**类型枚举**：`docx` / `pdf` / `pptx` / `xlsx`

---

## 2. 资料内容摘要

> 逐份文档按自身章节结构做摘要。每条摘要标注章节号（`D编号，§章节`），后面任何人想核实某个点，直接定位回原文对应位置即可。

### D1：`<例：产品需求文档_v2.3.docx>`

> `<一句话概括这份文档>` — 来源：`<产品经理>`

| 章节 | 内容摘要 |
| --- | --- |
| §1.1 业务现状 | `<例：当前客服系统日均处理 N 万通会话，纯人工模式>` |
| §1.2 用户规模 | `<例：坐席 300 人，覆盖 3 个业务线>` |
| §2.1 核心痛点 | `<例：风险事件识别滞后，事件占比 x%，滞后 ≥ N 分钟>` |
| §2.2 新人上手 | `<例：平均 N 周>` |
| §3.1 实时风险识别推送 | `<例：在通话中识别风险事件并推送给坐席，含 N 类风险模型>` |
| §3.2 自动工单生成 | `<例：通话结束后自动生成结构化工单，字段含 A/B/C>` |
| §5.1 并发用户数 | `<例：目标并发用户数 500>` |
| §5.3 性能要求 | `<例：核心链路 P99 ≤ 200ms>` |

### D2：`<例：竞品分析.pdf>`

> `<一句话概括这份文档>` — 来源：`<业务负责人>`

| 章节 | 内容摘要 |
| --- | --- |
| §2.1 竞品A 能力对标 | `<例：竞品 A 实时辅助准确率 ≥ 90%，支持 N 类风险场景>` |
| §2.3 合规要求 | `<例：新规要求通话 100% 留痕，当前方案不满足>` |
| §4.1 部署形态 | `<例：竞品均支持私有化部署，数据不出域>` |
| §4.2 合规留存 | `<例：通话记录需留存 ≥ 3 年，支持审计回溯>` |
| §5.2 MVP范围 | `<例：建议 MVP 不含移动端，待决策>` |

### D3：`<例：技术方案汇报.pptx>`

> `<一句话概括这份文档>` — 来源：`<技术团队>`

| 章节 | 内容摘要 |
| --- | --- |
| p.5 系统定位 | `<例：本系统定位为客服工作台的核心辅助模块>` |
| p.8 管理者实时干预 | `<例：管理者可实时查看并介入高风险会话>` |
| p.10 并发用户数 | `<例：预估并发 1000，与 D1 的 500 存在出入>` |
| p.12 坐席并发定义 | `<例：单坐席同时处理会话数，峰值 N>` |
| p.14 上下文字段 | `<例：建议上下文含文本+意图标签，与 D2 的仅文本不同>` |
| p.15 AI 能力策略 | `<例：提到 AI 能力可自研或采购，未决策>` |

---

## 3. 冲突记录

> 不同资料对同一事实描述矛盾时，**并列保留两个版本**，不做裁决。

| 编号 | 冲突主题 | 版本 A | 出处 A | 版本 B | 出处 B | 差异说明 |
| --- | --- | --- | --- | --- | --- | --- |
| X1 | `<例：目标并发用户数>` | 500 | `<D1，§5.1>` | 1000 | `<D3，p.10>` | 相差 1 倍，可能口径不同 |
| X2 | `<例：上下文字段范围>` | `<仅文本>` | `<D2，§3.1>` | `<文本+意图标签>` | `<D3，p.14>` | 字段维度差异 |

---

## 4. 硬指标清单

| 章节 | 硬指标 | 状态 |
| --- | --- | --- |
| §1 | 每份资料有解析状态，失败/跳过注明原因 | ✅ / ❌ |
| §2 | 每份文档按章节逐条摘要，每条标注了 `D编号，§章节` | ✅ / ❌ |
| §3 | 冲突信息并列保留，不做裁决 | ✅ / ❌ |

---

## 附录 A：生成流程

### 流程总览

| 步骤 | 动作 | 落入章节 |
| --- | --- | --- |
| Step0 | 读取模板 + 全部原始资料 | — |
| Step1 | 盘点资料清单，标注解析状态 | §1 |
| Step2 | 逐份打开资料，按自身章节结构逐条摘要 | §2 |
| Step3 | 交叉比对不同资料，发现并记录矛盾 | §3 |
| Step4 | 逐项核验硬指标 | §4 |

```mermaid
flowchart LR
    S0[读取模板与资料] --> S1[盘点资料清单]
    S1 --> S2[逐份精读逐章节摘要]
    S2 --> S3[交叉比对记录冲突]
    S3 --> S4[硬指标自检]
```

### 整理原则

1. **逐份精读，不跨文档归并**：摘要按文档自身章节结构组织，不做跨文档的主题重组（那是下游的事）
2. **出处即章节号**：每条摘要标注 `D编号，§章节`，直接映射回原文位置
3. **冲突保留**：矛盾信息并列保留两个版本，不擅自裁决
4. **事实驱动**：以原始资料中的事实为准，不添加主观推断

---

## 附录 B：解析 Skill

- `docx`：Word 类产品/业务文档
- `pdf`：PDF 类规范、手册、报告
- `pptx`：PPT 类方案/汇报
- `xlsx`：Excel 类数据清单、指标表
