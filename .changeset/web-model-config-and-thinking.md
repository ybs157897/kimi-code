---
"@moonshot-ai/kimi-code": minor
---

web: Add provider and model configuration to the web UI — create or edit third-party providers (name, type, API key, base URL) with a per-model list including context sizes — plus a thinking-level picker next to the composer's model selector.
