---
"@moonshot-ai/kimi-code": minor
---

Enable expert teams by default and load team packages dropped into `.kimi-code/experts/` (project) or `~/.kimi-code/experts/` (user) without installing a plugin. Run /experts to pick a team.
