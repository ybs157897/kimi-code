---
"@moonshot-ai/kimi-code": minor
---

web: Add an expert-team picker to the composer Modes menu, attach the selected team to one prompt with a visible mention, then return to the standard agent when that turn ends. Drop-in team packages under `.kimi-code/experts/` are discovered by the web backend too.
