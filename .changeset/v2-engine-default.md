---
"@moonshot-ai/kimi-code": major
---

Default to the v2 engine for all CLI modes (interactive TUI, `kimi -p`, and subcommands). `--agent` and `--agent-file` are now available in `kimi -p` without setting `KIMI_CODE_EXPERIMENTAL_FLAG`.
