---
"@moonshot-ai/kimi-code": minor
---

Add code-based extensions that can register tools, slash commands, and event hooks. Place TypeScript files under `.kimi-code/extensions/` and run `/reload` after edits.
